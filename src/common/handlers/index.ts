import { LyricsCreatedHandler } from './lyrics-created.handler';
import { LyricsVerifiedHandler } from './lyrics-verified.handler';

export const LyricsEventHandlers = [
  LyricsCreatedHandler,
  LyricsVerifiedHandler,
  // Add other handlers here as they are implemented
];